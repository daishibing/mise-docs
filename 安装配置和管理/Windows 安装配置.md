# Windows 安装配置

在 Windows 系统安装配置 mise

# 安装 mise

使用 WinGet 安装 mise：

```shell
winget install jdx.mise
```

# 配置环境变量

将 mise 的 shims 添加到用户 `Path` 环境变量：

```text
%LOCALAPPDATA%\mise\shims
```

> 配置完成后，重新打开终端使配置生效


