# 项目环境管理

使用 mise 管理项目中的工具版本，进入项目目录会自动切换对应的工具版本

# 设置项目工具版本

在项目目录中设置工具版本：

```shell
mise use <tool>@<version>
```

> 会在项目目录中生成或修改 `mise.toml`

# 初始化项目环境

进入项目目录后，会根据项目中的 `mise.toml` 安装项目所需的工具版本：

```shell
mise install
```


